import {createUser, updateUser, removeUser, login} from './user'
import {createAds, updateAds, removeAds} from './ads'

export {
  createUser, updateUser, removeUser, login,
  createAds, updateAds, removeAds
}